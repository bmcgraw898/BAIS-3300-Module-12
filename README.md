# BAIS-3300-Module-12
We will build two distinct Python environments - 1.) API server running on Flask that accepts inputs as JSON, runs the model, and returns the prediction as JSON and 2.) front end application running on a different instance of Flask that has a web form for users to make selections for input values.
